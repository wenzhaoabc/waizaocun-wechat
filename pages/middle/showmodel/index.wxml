<!--pages/showmodel/index.wxml-->

<view>
    <xr-model
        modelURL="{{modelURL}}"
        disable-scroll 
        id="main-frame"
        width="{{renderWidth}}" 
        height="{{renderHeight}}" 
        style="width:{{width}}px;height:{{height}}px;" 
        />
</view>